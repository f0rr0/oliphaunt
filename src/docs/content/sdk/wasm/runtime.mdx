---
title: Rust WASIX Runtime Guide
description: Understand Rust WASIX storage, initialization, locking, startup, supported hosts, and server compatibility.
---

# Rust WASIX Runtime Guide

`oliphaunt-wasix` is the Rust host for the portable WASIX runtime. Its storage,
assets, extensions, and server APIs are separate from native SDKs. The
[WASIX TypeScript binding](./browser-typescript) is another WASIX host with a
smaller direct-or-worker API for browsers, Node.js, Bun, and Deno.

<WasmRuntimeMap />

## Choose A Mode

Use `Oliphaunt` when your Rust code owns the database calls:

- direct function and method calls;
- no socket listener;
- best fit for tests, commands, jobs, and app-owned Rust state.

Use `OliphauntServer` when a library expects a PostgreSQL URI:

- SQLx, Diesel, SeaORM, `tokio-postgres`, or cross-language clients;
- local TCP or Unix socket listener;
- compatibility with existing Postgres clients inside the selected WASIX host.

Both modes share the same embedded WASIX backend model.

## Storage and initialization

Direct and server builders expose the same storage choices:

- `DatabaseStorage::Memory` is the default and keeps PGDATA in Wasmer's memory
  filesystem;
- `Directory(path)` is caller-owned persistent host storage;
- `ApplicationData(identity)` resolves a retained platform app-data directory;
- `TemporaryDirectory` allocates and owns a disposable host directory.

Storage says where mutable files live. `DatabaseInitialization` independently
chooses the packaged template, a fresh WASIX `initdb`, or a compatible physical
archive. Reopening initialized persistent storage validates and reuses it.

## Operational Limits

The WASIX runtime model owns one embedded backend per open database:

- one `Oliphaunt` instance owns one embedded backend;
- one `OliphauntServer` exposes one embedded backend;
- downstream client pools use one connection;
- server mode gives local PostgreSQL client compatibility inside the selected
  WASIX host.

Generated server URLs include `sslmode=disable`. `CancelRequest` and normal
startup packets are supported. The server-compatible API still runs against the
embedded WASIX backend for that database.

## Persistent-storage locking and lifecycle

Persistent directories are locked while open. A second direct or server open
against the same directory returns a lock error so one runtime owns it at a time.

Close database clients before calling `OliphauntServer::shutdown()`. The server
thread waits for active client work to finish before exiting.

Use `backup()` and initialize a new database with
`DatabaseInitialization::PhysicalArchive(bytes)`, or use `try_clone()`, for a
same-version physical clone. Use logical dumps through `pg_dump` for portable exports and upgrades.

## Startup And Preload

The WASIX runtime opens only when the application package contains the matching
portable runtime artifact and the target-specific AOT artifact. Public consumer
builds receive those artifacts through package-manager dependencies, not through
runtime downloads or app-owned archive environment variables.

The crate exposes two preload hooks:

```rust,no_run
use oliphaunt_wasix::Oliphaunt;

fn main() -> Result<(), Box<dyn std::error::Error>> {
    Oliphaunt::preload()?;
    Ok(())
}
```

Call `preload` before a visible startup path when you want to warm the shipped
runtime and target AOT artifact.

Startup configuration belongs on the builders:

- `postgres_config(...)` for PostgreSQL GUCs;
- `username(...)` and `database(...)` for the session target;
- `relaxed_durability(true)` for cacheable local workloads;
- `startup_arg(...)` for advanced startup arguments.

## Supported Targets

Release assets are built for:

- macOS arm64;
- Linux x64;
- Linux arm64;
- Windows x64.

Read `engine_capabilities()` and package reports before enabling target-specific
UI. A host target needs matching shipped runtime assets for the WASIX runtime
to open.

`oliphaunt-wasix` is the Rust host package. The separate
[`@oliphaunt/wasix-ts` TypeScript binding](./browser-typescript) runs the portable
guest with direct or worker-isolated execution in browsers, Node.js, Bun, and
Deno, and defaults to memory. IndexedDB is a selective browser adapter. Node,
Bun, and Deno selectively expose
snapshot-backed directory persistence through the matching `storage/node`,
`storage/bun`, or `storage/deno` subpath.
Shared runtime carriers do not imply API parity, and the package
does not route through the TypeScript product's native runtime.

## Server-Compatible Access

Reach for `OliphauntServer` when you need client-library compatibility:

- SQLx migrations and query APIs;
- ORMs that expect a PostgreSQL URI;
- Python, Go, or Node clients in local app tests;
- local tools that already speak the Postgres wire protocol.

Reach for `Oliphaunt` when you control the Rust call site. It avoids the socket
layer and keeps the API surface smaller.
