$schema: "https://moonrepo.dev/schemas/project.json"

id: "oliphaunt-wasix-ts"
language: "typescript"
layer: "library"
stack: "frontend"
tags: ["binding", "wasix", "wasm", "typescript", "browser", "node", "bun", "deno", "sdk", "release-product"]
dependsOn:
  - id: "postgres-protocol-transport-contract"
    scope: "development"
  - id: "postgres-tool-output-contract"
    scope: "development"
  - id: "cluster-seed-contract"
    scope: "development"
  - id: "shared-test-fixtures"
    scope: "development"
  - "liboliphaunt-wasix"
  - "oliphaunt-wasix-napi"

project:
  title: "Oliphaunt WASIX TypeScript binding"
  description: "Universal WASIX TypeScript binding with browser and native host placements."
  owner: "oliphaunt"
  release:
    component: "oliphaunt-wasix-ts"
    packagePath: "src/bindings/wasix-ts"

owners:
  defaultOwner: "@oliphaunt/sdk-js"

fileGroups:
  code:
    - "**/*"
    - "!**/*.md"
    - "!moon.yml"
    - "!release.toml"

tasks:
  compile:
    tags: ["quality", "static"]
    command: "pnpm --dir src/bindings/wasix-ts typecheck"
    deps:
      - "shared-js-core:build"
    inputs:
      - "@group(pnpm-workspace)"
      - project: "shared-js-core"
        group: "sources"
      - "@group(code)"
    options:
      cache: true
      runFromWorkspaceRoot: true

  unit:
    tags: ["quality", "unit"]
    command: "pnpm --dir src/bindings/wasix-ts test"
    deps:
      - "shared-js-core:build"
    inputs:
      - "@group(pnpm-workspace)"
      - project: "shared-js-core"
        group: "sources"
      - project: "shared-test-fixtures"
        group: "fixtures"
      - project: "cluster-seed-contract"
        group: "contract"
      - "@group(code)"
    options:
      cache: true
      runFromWorkspaceRoot: true
      runInCI: false

  browser-host:
    tags: ["artifact", "build", "requires-rust"]
    command: "bash src/bindings/wasix-ts/host/build-sdk.sh"
    deps:
      - "oliphaunt-wasix-ts:host-contract"
    env:
      WASM_PACK_VERSION: "0.15.0"
    inputs:
      - "host/**/*"
      - "@group(cargo-workspace)"
      - "/src/shared/postgres-protocol-transport-contract/contract.json"
      - "/src/shared/postgres-tool-output-contract/contract.json"
    outputs:
      - "/target/oliphaunt-wasix-ts/host/wasmer-sdk/**/*"
    options:
      cache: true
      runFromWorkspaceRoot: true

  host-contract:
    tags: ["quality", "static", "contract"]
    command: "node --test src/bindings/wasix-ts/host/*.test.mjs"
    inputs:
      - "host/**/*"
      - "src/direct-client-common.ts"
      - "/src/shared/postgres-protocol-transport-contract/**/*"
      - "/src/shared/postgres-tool-output-contract/**/*"
      - "/src/bindings/wasix-rust/crates/oliphaunt-wasix/src/oliphaunt/postgres_mod.rs"
      - "/src/runtimes/liboliphaunt/wasix/assets/build/postgres/patches/**/*"
      - "/src/runtimes/liboliphaunt/wasix/assets/build/wasix_shim/**/*"
    options:
      cache: true
      runFromWorkspaceRoot: true

  package:
    tags: ["package"]
    script: |
      set -e
      pnpm --dir src/bindings/wasix-ts build
      node src/bindings/wasix-ts/tools/stage-host.mjs
      node src/bindings/wasix-ts/tools/package.mjs target/oliphaunt-wasix-ts/package
    deps:
      - "oliphaunt-wasix-ts:browser-host"
      - "shared-js-core:build"
    inputs:
      - "@group(legal-files)"
      - "@group(pnpm-workspace)"
      - project: "shared-js-core"
        group: "sources"
      - "**/*"
    outputs:
      - "/target/oliphaunt-wasix-ts/package/**/*"
      - "/src/bindings/wasix-ts/lib/**/*"
    options:
      cache: true
      runFromWorkspaceRoot: true

  qualify:
    tags: ["release", "package"]
    command: "true"
    deps:
      - "oliphaunt-wasix-ts:compile"
      - "oliphaunt-wasix-ts:unit"
      - "oliphaunt-wasix-ts:package"
    inputs: []
    options:
      cache: true
      runFromWorkspaceRoot: true
      runInCI: false
