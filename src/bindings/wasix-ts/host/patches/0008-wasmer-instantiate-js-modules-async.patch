diff --git a/Cargo.toml b/Cargo.toml
--- a/Cargo.toml
+++ b/Cargo.toml
@@ -363,6 +363,9 @@
 [target.'cfg(target_arch = "wasm32")'.dependencies.wasm-bindgen]
 version = "0.2.100"

+[target.'cfg(target_arch = "wasm32")'.dependencies.wasm-bindgen-futures]
+version = "0.4"
+
 [target.'cfg(target_arch = "wasm32")'.dependencies.wasmer-compiler]
 version = "=6.1.0"

diff --git a/src/backend/js/entities/instance.rs b/src/backend/js/entities/instance.rs
--- a/src/backend/js/entities/instance.rs
+++ b/src/backend/js/entities/instance.rs
@@ -19,6 +19,21 @@ pub struct Instance {
 // unsafe impl Send for Instance {}

 impl Instance {
+    #[cfg(target_arch = "wasm32")]
+    pub(crate) async fn new_async(
+        mut store: &mut impl AsStoreMut,
+        module: &Module,
+        imports: &Imports,
+    ) -> Result<(Self, Exports), InstantiationError> {
+        let instance = module
+            .as_js()
+            .instantiate_async(&mut store, imports)
+            .await
+            .map_err(InstantiationError::Start)?;
+
+        Self::from_module_and_instance(store, module, instance)
+    }
+
     pub(crate) fn new(
         mut store: &mut impl AsStoreMut,
         module: &Module,
@@ -29,7 +44,7 @@ impl Instance {
             .instantiate(&mut store, imports)
             .map_err(|e| InstantiationError::Start(e))?;

-        Self::from_module_and_instance(store, &module, instance)
+        Self::from_module_and_instance(store, module, instance)
     }

     pub(crate) fn new_by_index(
diff --git a/src/backend/js/entities/module.rs b/src/backend/js/entities/module.rs
--- a/src/backend/js/entities/module.rs
+++ b/src/backend/js/entities/module.rs
@@ -3,7 +3,11 @@ use crate::backend::js::vm::{VMExtern, VMInstance};
 use bytes::Bytes;
 use js_sys::{Reflect, Uint8Array, WebAssembly};
 use tracing::{debug, warn};
-use wasm_bindgen::{prelude::*, JsValue};
+use wasm_bindgen::{prelude::*, JsValue};
+#[cfg(target_arch = "wasm32")]
+use wasm_bindgen::JsCast;
+#[cfg(target_arch = "wasm32")]
+use wasm_bindgen_futures::JsFuture;
 use wasmer_types::{
     CompileError, DeserializeError, ExportType, ExportsIterator, ExternType, FunctionType,
     GlobalType, ImportType, ImportsIterator, MemoryType, ModuleInfo, Mutability, Pages,
@@ -139,6 +143,32 @@ impl Module {
         store: &mut impl AsStoreMut,
         imports: &Imports,
     ) -> Result<VMInstance, RuntimeError> {
+        let (imports_object, _import_externs) = self.imports_object(store, imports)?;
+        Ok(WebAssembly::Instance::new(&self.module, &imports_object)
+            .map_err(|e: JsValue| -> RuntimeError { e.into() })?)
+    }
+
+    #[cfg(target_arch = "wasm32")]
+    pub(crate) async fn instantiate_async(
+        &self,
+        store: &mut impl AsStoreMut,
+        imports: &Imports,
+    ) -> Result<VMInstance, RuntimeError> {
+        let (imports_object, _import_externs) = self.imports_object(store, imports)?;
+        let instance = JsFuture::from(WebAssembly::instantiate_module(
+            &self.module,
+            &imports_object,
+        ))
+        .await
+        .map_err(|e: JsValue| -> RuntimeError { e.into() })?;
+        Ok(instance.unchecked_into())
+    }
+
+    fn imports_object(
+        &self,
+        store: &mut impl AsStoreMut,
+        imports: &Imports,
+    ) -> Result<(js_sys::Object, Vec<Extern>), RuntimeError> {
         // Ensure all imports come from the same store.
         if imports
             .into_iter()
@@ -205,8 +235,7 @@ impl Module {
             // in case the import is not found, the JS Wasm VM will handle
             // the error for us, so we don't need to handle it
         }
-        Ok(WebAssembly::Instance::new(&self.module, &imports_object)
-            .map_err(|e: JsValue| -> RuntimeError { e.into() })?)
+        Ok((imports_object, import_externs))
     }

     pub fn name(&self) -> Option<&str> {
diff --git a/src/entities/instance.rs b/src/entities/instance.rs
--- a/src/entities/instance.rs
+++ b/src/entities/instance.rs
@@ -20,6 +20,59 @@ pub struct Instance {
 }

 impl Instance {
+    /// Asynchronously creates a new JavaScript-backed WebAssembly instance.
+    ///
+    /// This uses `WebAssembly.instantiate()` so browsers can instantiate large
+    /// modules without hitting the synchronous constructor's size limit. Other
+    /// enabled backends retain their regular synchronous instantiation path.
+    #[cfg(all(feature = "js", target_arch = "wasm32"))]
+    #[allow(clippy::result_large_err)]
+    pub async fn new_async(
+        store: &mut impl AsStoreMut,
+        module: &Module,
+        imports: &Imports,
+    ) -> Result<Self, InstantiationError> {
+        let (_inner, exports) = match &store.as_store_mut().inner.store {
+            #[cfg(feature = "sys")]
+            crate::BackendStore::Sys(_) => {
+                let (i, e) = crate::backend::sys::instance::Instance::new(store, module, imports)?;
+                (crate::BackendInstance::Sys(i), e)
+            }
+            #[cfg(feature = "wamr")]
+            crate::BackendStore::Wamr(_) => {
+                let (i, e) = crate::backend::wamr::instance::Instance::new(store, module, imports)?;
+                (crate::BackendInstance::Wamr(i), e)
+            }
+            #[cfg(feature = "wasmi")]
+            crate::BackendStore::Wasmi(_) => {
+                let (i, e) = crate::backend::wasmi::instance::Instance::new(store, module, imports)?;
+                (crate::BackendInstance::Wasmi(i), e)
+            }
+            #[cfg(feature = "v8")]
+            crate::BackendStore::V8(_) => {
+                let (i, e) = crate::backend::v8::instance::Instance::new(store, module, imports)?;
+                (crate::BackendInstance::V8(i), e)
+            }
+            crate::BackendStore::Js(_) => {
+                let (i, e) =
+                    crate::backend::js::instance::Instance::new_async(store, module, imports)
+                        .await?;
+                (crate::BackendInstance::Js(i), e)
+            }
+            #[cfg(feature = "jsc")]
+            crate::BackendStore::Jsc(_) => {
+                let (i, e) = crate::backend::jsc::instance::Instance::new(store, module, imports)?;
+                (crate::BackendInstance::Jsc(i), e)
+            }
+        };
+
+        Ok(Self {
+            _inner,
+            module: module.clone(),
+            exports,
+        })
+    }
+
     /// Creates a new `Instance` from a WebAssembly [`Module`] and a
     /// set of imports using [`Imports`] or the [`imports!`] macro helper.
     ///
