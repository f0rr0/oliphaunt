// Checkout builds consume the runtime-owned ABI; distribution staging replaces
// this forwarding header with the canonical header for standalone consumers.
#include "../../../../../runtime/include/oliphaunt.h"
