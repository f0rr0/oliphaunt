# `@oliphaunt/wasix-napi-linux-arm64-gnu`

Prebuilt Node-API 8 addon for Oliphaunt WASIX on Linux arm64 with glibc. This
optional carrier embeds both the standard and ICU database profiles in one
binary and has no install script.
