# Oliphaunt resource descriptors

Shared data types for SDKs and optional resource packages. Applications use the descriptors exported by their chosen extension or ICU package; no direct dependency on this crate is needed. This crate contains no runtime binaries or resource payloads.
