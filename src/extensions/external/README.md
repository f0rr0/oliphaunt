# Shared External Extension Recipes

This directory is the tracked source root for shared external extension recipe
metadata. Per-extension recipes move here as they graduate from the transitional
catalog rows to source-owned recipes.

Do not place generated checkouts here. Source checkouts stay under
`target/oliphaunt-sources/checkouts/`.
