# Native Baselines

Committed native benchmark baselines live here when release evidence is
intentionally promoted. Local and CI-generated reports stay under `target/perf`
unless they are promoted as release artifacts.
