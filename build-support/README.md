# Build Support

Shared build definitions and templates belong here when they are not a product
package and not an executable tool.

Examples:

- cross-platform native artifact metadata templates;
- CI-local environment setup templates;
- shared packaging schemas consumed by more than one SDK.

Executable automation belongs in `tools/`.
