name: extension-artifacts-native
on:
  workflow_call:
    inputs:
      matrix:
        required: true
        type: string
      job-targets:
        required: true
        type: string
permissions:
  contents: read
env:
  OLIPHAUNT_CI_JOB_TARGETS_JSON: ${{ inputs.job-targets }}
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  HEAVY_CACHE_SAVE_IF: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
defaults:
  run:
    shell: bash
jobs:
  build:
    name: Builds / Native Extension Artifacts (${{ matrix.target }})
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(inputs.matrix) }}
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 135
    env:
      CCACHE_DIR: ${{ github.workspace }}/.ci-cache/ccache/native-extension/${{ matrix.target }}
      CCACHE_BASEDIR: ${{ github.workspace }}
      CCACHE_COMPILERCHECK: content
      CCACHE_COMPRESS: "true"
      OLIPHAUNT_CCACHE_MAX_SIZE: ${{ matrix.target == 'ios-xcframework' && '512M' || '2G' }}
      OLIPHAUNT_CCACHE_ZERO_STATS: "1"
    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false

      - name: Set up Moon
        uses: ./.github/actions/setup-moon

      - name: Set up Apple
        if: ${{ runner.os == 'macOS' }}
        uses: ./.github/actions/setup-apple

      - name: Set up Android
        if: ${{ startsWith(matrix.target, 'android-') }}
        uses: ./.github/actions/setup-android
        with:
          gradle-cache: "false"

      - name: Set up MSVC
        if: ${{ runner.os == 'Windows' }}
        uses: ./.github/actions/setup-msvc

      - name: Verify Windows VC runtime atomic staging
        if: ${{ runner.os == 'Windows' }}
        run: tools/dev/bun.sh test ./tools/packaging/windows-vc-runtime-closure.test.mts

      - name: Set up Rust
        uses: ./.github/actions/setup-rust

      - name: Prepare native compiler cache path
        if: ${{ matrix.target == 'ios-xcframework' }}
        run: mkdir -p "$CCACHE_DIR"

      - name: Restore native compiler cache
        id: restore_ios_extension_ccache
        if: ${{ matrix.target == 'ios-xcframework' }}
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: ${{ env.CCACHE_DIR }}
          key: liboliphaunt-native-extension-ccache-v2-${{ matrix.target }}-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('.github/actions/setup-apple/**', '.github/scripts/setup-native-build-tools.sh', 'third-party/postgres/**', 'third-party/icu/**', 'third-party/openssl/**', 'runtimes/liboliphaunt-native/sources/**', 'extensions/contracts/**', 'extensions/catalog/extensions.source.json', 'extensions/catalog/native-components.toml', 'extensions/external/postgis/tools/preprocess-sql.mts', 'extensions/contrib/postgres18.toml', 'extensions/external/*/source.toml', 'extensions/external/*/recipe.toml', 'extensions/external/*/dependencies/**/source.toml', 'extensions/external/*/dependencies/**/recipe.toml', 'extensions/external/*/patches/**', 'extensions/external/*/dependencies/**/patches/**', 'extensions/generated/extensions.catalog.json', 'extensions/generated/contrib-build.tsv', 'extensions/generated/pgxs-build.tsv', 'extensions/generated/mobile/static-extensions.tsv', 'extensions/generated/mobile/static-registry.json', 'runtimes/liboliphaunt-native/bin/build-*.sh', '!runtimes/liboliphaunt-native/bin/*.test.sh', 'extensions/artifacts/native/tools/build-windows-extensions.sh', 'extensions/artifacts/native/tools/windows-extension-sources.mts', 'extensions/external/postgis/tools/windows/**', 'runtimes/liboliphaunt-native/bin/build-output.bash', 'runtimes/liboliphaunt-native/bin/common.sh', 'runtimes/liboliphaunt-native/bin/fetch-pinned-git-checkout.sh', 'third-party/icu/tools/build.sh', 'runtimes/liboliphaunt-native/bin/mobile-*.sh', 'runtimes/liboliphaunt-native/bin/postgis-dependency-cache.sh', 'runtimes/liboliphaunt-native/bin/postgres-backend-objects.mk', 'postgres-tools/native/crates/tools/Cargo.toml', 'postgres-tools/native/crates/tools/build.rs', 'postgres-tools/native/crates/tools/src/**', 'runtimes/liboliphaunt-native/include/**', 'runtimes/liboliphaunt-native/patches/**', 'runtimes/liboliphaunt-native/portable-uuid/**', 'runtimes/liboliphaunt-native/postgres18/**', 'runtimes/liboliphaunt-native/src/**') }}
          restore-keys: |
            liboliphaunt-native-extension-ccache-v2-${{ matrix.target }}-${{ runner.os }}-${{ runner.arch }}-

      - name: Configure native compiler cache
        run: .github/scripts/setup-native-build-tools.sh

      - name: Build native exact-extension artifacts
        timeout-minutes: 120
        env:
          OLIPHAUNT_EXTENSION_PRODUCTS: ${{ matrix.extensions_csv }}
          OLIPHAUNT_EXTENSION_TARGET: ${{ matrix.target }}
          OLIPHAUNT_BISON: /opt/homebrew/opt/bison/bin/bison
        run: .github/scripts/run-planned-moon-job.sh extension-artifacts-native

      - name: Validate produced iOS extension carriers
        id: validate_ios_extension_carriers
        if: ${{ matrix.target == 'ios-xcframework' }}
        run: bun runtimes/liboliphaunt-native/tools/validate-ios-carrier-zips.mts --root target/extensions/native/release-assets/ios-xcframework

      - name: Save bounded iOS exact-extension compiler cache
        id: save_ios_extension_ccache
        if: ${{ matrix.target == 'ios-xcframework' && env.HEAVY_CACHE_SAVE_IF == 'true' && steps.restore_ios_extension_ccache.outputs.cache-hit != 'true' }}
        continue-on-error: true
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9
        with:
          path: ${{ env.CCACHE_DIR }}
          key: liboliphaunt-native-extension-ccache-v2-${{ matrix.target }}-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('.github/actions/setup-apple/**', '.github/scripts/setup-native-build-tools.sh', 'third-party/postgres/**', 'third-party/icu/**', 'third-party/openssl/**', 'runtimes/liboliphaunt-native/sources/**', 'extensions/contracts/**', 'extensions/catalog/extensions.source.json', 'extensions/catalog/native-components.toml', 'extensions/external/postgis/tools/preprocess-sql.mts', 'extensions/contrib/postgres18.toml', 'extensions/external/*/source.toml', 'extensions/external/*/recipe.toml', 'extensions/external/*/dependencies/**/source.toml', 'extensions/external/*/dependencies/**/recipe.toml', 'extensions/external/*/patches/**', 'extensions/external/*/dependencies/**/patches/**', 'extensions/generated/extensions.catalog.json', 'extensions/generated/contrib-build.tsv', 'extensions/generated/pgxs-build.tsv', 'extensions/generated/mobile/static-extensions.tsv', 'extensions/generated/mobile/static-registry.json', 'runtimes/liboliphaunt-native/bin/build-*.sh', '!runtimes/liboliphaunt-native/bin/*.test.sh', 'extensions/artifacts/native/tools/build-windows-extensions.sh', 'extensions/artifacts/native/tools/windows-extension-sources.mts', 'extensions/external/postgis/tools/windows/**', 'runtimes/liboliphaunt-native/bin/build-output.bash', 'runtimes/liboliphaunt-native/bin/common.sh', 'runtimes/liboliphaunt-native/bin/fetch-pinned-git-checkout.sh', 'third-party/icu/tools/build.sh', 'runtimes/liboliphaunt-native/bin/mobile-*.sh', 'runtimes/liboliphaunt-native/bin/postgis-dependency-cache.sh', 'runtimes/liboliphaunt-native/bin/postgres-backend-objects.mk', 'postgres-tools/native/crates/tools/Cargo.toml', 'postgres-tools/native/crates/tools/build.rs', 'postgres-tools/native/crates/tools/src/**', 'runtimes/liboliphaunt-native/include/**', 'runtimes/liboliphaunt-native/patches/**', 'runtimes/liboliphaunt-native/portable-uuid/**', 'runtimes/liboliphaunt-native/postgres18/**', 'runtimes/liboliphaunt-native/src/**') }}

      - name: Show native compiler cache stats
        if: ${{ always() && runner.os != 'Windows' }}
        run: |
          if command -v ccache >/dev/null 2>&1; then
            ccache --show-stats
          else
            echo "ccache was not installed before the build stopped"
          fi

      - name: Upload native exact-extension artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: liboliphaunt-native-extension-artifacts-${{ matrix.target }}
          path: target/extensions/native/release-assets
          if-no-files-found: error

      - name: Upload native exact-extension build logs
        id: upload_native_extension_logs
        if: ${{ failure() || cancelled() }}
        timeout-minutes: 5
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: liboliphaunt-native-extension-logs-${{ matrix.target }}
          path: |
            target/liboliphaunt-mobile-extension-ci/**/*.log
            target/liboliphaunt-mobile-extension-release/**/*.log
            target/liboliphaunt-mobile-extension-release/ios-xcframework/ios-simulator/*.log
            target/liboliphaunt-mobile-extension-release/ios-xcframework/ios-device/*.log
            target/liboliphaunt-pg18-extension-release/**/*.log
            target/liboliphaunt-pg18-*-extension-release/**/*.log
            /tmp/liboliphaunt-ci-*-extensions.log
            /tmp/liboliphaunt-release-*-extensions.log
            /tmp/liboliphaunt-ci-extension-assets-fetch.log
            /tmp/liboliphaunt-release-extension-assets-fetch.log
          if-no-files-found: warn

