$schema: https://moonrepo.dev/schemas/project.json
id: artifact-packaging
language: typescript
layer: library
stack: systems
tags:
  - javascript-quality
  - shared
  - artifacts
  - packaging
project:
  title: Artifact Packaging
  description: "Deterministic archive creation, reading, and safe staging shared by product packagers."
  owner: oliphaunt
owners:
  defaultOwner: "@oliphaunt/core"
  paths:
    "**/*":
      - "@oliphaunt/core"
fileGroups:
  source:
    - "*.{mjs,mts}"
    - "!*.test.{mjs,mts}"
tasks:
  test:
    tags:
      - quality
      - unit
      - requires-rust
    command: "bash tools/packaging/test.sh"
    inputs:
      - "/tools/dev/bun.sh"
      - "@group(release-target-contract)"
      - "@group(package-test-metadata)"
      - "@group(legal-files)"
      - /src/extensions/tools/extension-upstream-licenses.mts
      - /src/extensions/external/**/upstream-license-data.json
      - "@group(upstream-licenses)"
      - "/src/{native,wasix}/runtime/THIRD_PARTY_NOTICES.md"
      - "/src/third-party/{icu,openssl}/source.toml"
      - "**/*.mts"
      - "**/*.sh"
      - /src/database-resources/contracts/*.mts
      - /tools/release/**/*.mts
      - /tools/packaging/windows-vc-runtime-policy.json
      - /src/wasix/runtime/crates/**/*
      - /src/wasix/postgres-tools/crates/**/*
      - /src/database-resources/icu/cargo/**/*
    options:
      cache: true
      runFromWorkspaceRoot: true
